# Pinned for the Docker image (python:3.12-slim). These all ship cp312 wheels,
# so the build needs no compiler and stays small (no model weights baked in).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Optional Gemini polish uses the REST API via Python stdlib urllib, so no
# model weights or provider SDK are needed in the image.
