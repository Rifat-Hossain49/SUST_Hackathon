# Pinned for the Docker image (python:3.12-slim). These all ship cp312 wheels,
# so the build needs no compiler and stays small (no model weights baked in).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Optional LLM polish (OFF by default). The SDK is small (no model weights);
# it is only imported when USE_LLM=true, so it never affects the default path.
anthropic==0.42.0
